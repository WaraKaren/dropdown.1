/* import React, { useState } from 'react';

export const search = () => {
  const [inputValue, setInputValue] = useState('');

  const handleInputChange = (event) => {
    const value = event.target.value.replace(/\D/g, ''); // Remover todos los caracteres que no sean números
    setInputValue(value);
  };
  return (
    <textarea
      placeholder="Ingrese números"
      value={inputValue}
      onChange={handleInputChange}
    />
  );
}
 */
